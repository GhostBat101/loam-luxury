/**
 * PostCSS Build Configuration.
 * Configures Tailwind CSS and Autoprefixer processors for CSS compilation.
 * Communicates with: tailwind.config.ts and src/styles/main.css.
 */

export default {
  plugins: {
    tailwindcss: {},
    autoprefixer: {}
  }
};
